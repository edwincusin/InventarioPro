import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
export default function ProductosScreen() {
  return (
    <SafeAreaView>
      <Text>ProductosScreen ProductosScreen</Text>
    </SafeAreaView>
  );
}